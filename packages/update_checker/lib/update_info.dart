class UpdateInfo {
  final int buildNumber;
  final String changelog;
  final String url;
  final bool isOptional;

  UpdateInfo({
    this.buildNumber,
    this.changelog,
    this.url,
    this.isOptional,
  });
}
