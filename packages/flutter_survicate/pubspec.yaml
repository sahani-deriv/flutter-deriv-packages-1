name: flutter_survicate
description: Flutter survicate plugin project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.deriv.survicate.flutter_survicate
        pluginClass: FlutterSurvicatePlugin
      ios:
        pluginClass: FlutterSurvicatePlugin
