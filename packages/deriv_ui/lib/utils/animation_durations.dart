/// Duration used for changing colors in animated widgets
const Duration colorChangeDuration = Duration(milliseconds: 300);

/// Duration used for changing size in animated widgets
const Duration sizeChangeDuration = Duration(milliseconds: 300);

/// Duration used for scrolling in animated widgets
const Duration scrollAnimationDuration = Duration(seconds: 1);

/// Duration used for auto scrolling in animated widgets
const Duration autoScrollInterval = Duration(seconds: 4);

/// Duration used for sliding page change navigation
const Duration slidingPageChangeDuration = Duration(milliseconds: 300);
