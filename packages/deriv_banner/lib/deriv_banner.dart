import 'package:flutter/material.dart';

/// Deriv Banner class.
class DerivBanner extends StatelessWidget {
  /// Initializes a [DerivBanner].
  const DerivBanner({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const Center(child: Text('Banner'));
}
