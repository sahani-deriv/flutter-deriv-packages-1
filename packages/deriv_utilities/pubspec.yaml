name: deriv_utilities
description: A package that contains the utilities including helper functions, mixins, and extensions.

version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
